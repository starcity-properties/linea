import  Drawing  from '../drawing.js';

export default class Feature extends Drawing {
    constructor(canvas) {
        super(canvas);
        // this.paper = canvas.paper;
        // this.features = [];
    }
};
